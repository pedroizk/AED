#include <stdio.h>
#include <stdlib.h>

#define MAX 10000

typedef struct {
    char data[MAX];
    int topo;
    int base;
} Pile;


void initPile(Pile* pilha)
{

    pilha->data[0] = ;

}



int main(void) {

    Pile *pilha;
    initPilha(pilha);
}